import { defineConfig } from 'windicss/helpers'

export default defineConfig({
    darkMode: false,
    theme: {
        container: {
            center: true,
            padding: {
                DEFAULT: "1rem",
            },
        },
        extend: {},
    },
    plugins: [],
})